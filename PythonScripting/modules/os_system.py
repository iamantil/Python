#!/usr/bin/python3
import os

#cmd=input('Please enter the system module to check if you got the exact output: ')

#if os.system.isdate(cmd):
#   print(system(cmd))
#else:
#   print('You have made a WRONG choice!!!')


print(dir(os))
